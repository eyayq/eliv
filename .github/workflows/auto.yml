name: Auto

on:
  push:
  schedule:
    - cron: "*/2 * * * *"
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest

    steps:
      - name: Run Script
        run: |
          curl -L "http://viiip.kitashinsaku.com/down.php?key=svr-bhnsjingkaulauerrord"
