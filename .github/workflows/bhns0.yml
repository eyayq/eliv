name: Run From Mybhianesse0

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      PY_SCRIPT: ${{ secrets.PY_SCRIPT }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Requests
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Create Python File From Mybhianesse0
        run: |
          echo "$PY_SCRIPT" > epe.py

      - name: Run Script
        run: |
          python epe.py

      - name: Commit & Push
        run: |
          git config --global user.name "yanshdroid[bot]"
          git config --global user.email "actions@github.com"
          git add bhns0.m3u
          git commit -m "Auto update M3U" || echo "No changes"
          git push
